# Frontend environment example. Copy to .env.local and adjust as needed.
# The Vite dev proxy already maps these to the running backend; you only
# need to set these when running outside the dev proxy (e.g. preview/build).
VITE_API_URL=/api/helpdesk
VITE_HUB_URL=/hubs/notifications
